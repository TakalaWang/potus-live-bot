apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: potus-live-bot-data
  namespace: potus-live-bot
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi # dedup state + transcripts (plain text), plenty of headroom
